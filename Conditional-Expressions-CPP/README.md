# Условные выражения в C++

**Автор:** Бобров  
**Дата:** 02.12.25  
**Уровень:** Начинающий  

Изучение условных выражений в C++ — операции сравнения и логические операции для принятия решений в программах.

## 📌 Что внутри:

### 1. Операции сравнения
- `==` — равно (equality)
- `!=` — не равно (not equal)
- `>` — больше (greater than)
- `<` — меньше (less than)
- `>=` — больше или равно
- `<=` — меньше или равно

### 2. Логические операции
- `!` — отрицание (NOT)
- `&&` — логическое И (AND)
- `||` — логическое ИЛИ (OR)
- `^` — исключающее ИЛИ (XOR)

### 3. Комбинирование выражений
- Объединение операций сравнения с логическими операторами
- Приоритет операций (сравнение → логические)
- Сокращённое вычисление (short-circuit evaluation)

## 🎯 Цель:
- Освоить все операции сравнения и логические операции
- Понимать приоритет выполнения операций
- Уметь комбинировать условия для сложных проверок
- Знать особенности вывода логических значений

## ⚠️ Важные моменты:
- Условные выражения возвращают `bool` (true/false)
- Для вывода true/false используйте `std::boolalpha`
- `&&` и `||` имеют сокращённое вычисление (short-circuit)
- Операции сравнения имеют приоритет над логическими

## 🔗 Ссылки:
- [Telegram-канал](https://t.me/Bobrov_Programmist)
- [Предыдущая тема: Операции присваивания]
- [Следующая тема: Конструкция if-else]

---

*Все примеры строго по учебным материалам. Без отсебятины.*
